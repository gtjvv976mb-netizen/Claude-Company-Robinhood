node_modules/
dist/

# secrets — never commit
.env
.env.local

# generated at runtime
claude-co.db
claude-co.db-*
reports/*.md
.codex-artifacts/

# editor/session-local: carries an absolute home-directory path
.claude/

# paper-trade.mjs caches GeckoTerminal responses so a rerun costs no rate limit
executor/.paper-cache/

# The guide recorder scratch: raw takes and synthesized voice. The finished
# media in token/ is committed; this is the workshop floor.
.guide-work/
